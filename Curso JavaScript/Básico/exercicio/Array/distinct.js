        
        
        for (var i=0, len=freteUnico.length; i<len; i++){
                
            if (out.indexOf(freteUnico[i]) === -1)
                out.push(freteUnico[i]);
            }
                        engine.debug(JSON.stringify(out))